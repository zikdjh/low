# 低代码平台引入「应用层（Application）」改造方案

> 文档版本：v1 · 2026-06-23
> 范围：`back/` Spring Boot 后端 + `front/` Vue 3 前端
> 状态：已与业主确认决策，待实施

---

## 1. Context（为什么要做）

当前低代码平台所有资源（实体、字段、页面、组件、字典、关系、工作流、业务模块）共用一个**全局命名空间**：

- 业务键 `EntityMeta.code` / `EntityMeta.tableName` / `PageSchema.pageCode` / `ComponentDef.compKey` / `DictType.dictCode` 在数据库层都是 `unique=true`；
- 所有 Repository 都是 `findByCode(String)` 形态，**不带任何 scope 参数**；
- 所有 controller 路径不带任何业务范围段（无 `app/tenant/namespace`）；
- 物理表名硬编码为 `lc_{code}`；
- 仓库根的 `LIGHTWEIGHT_SCOPE.md` 也明确把多租户列为 out-of-scope。

随着业务模块增多（已经有了 `leave`，后续会持续追加），必须引入「应用（Application）」概念来组织和隔离这些资源；同时保留一个内置 `common` 应用，承载跨应用共享的资源（系统组件、系统字典、公共实体等）。

### 目标

- 资源按 `appCode` 隔离，业务键唯一性由全局改为 **`(appCode, code)` 复合唯一**；
- 引入内置 `common` 应用，所有应用查询时按 **`(currentApp ∪ common)`** 聚合；
- 前端 URL 改为 `/lowcode/app/{appCode}/...`，提供应用切换/管理 UI；
- 现存数据安全迁移：所有旧元数据回填到内置 `default` 应用、系统组件/系统字典回填到 `common`；**物理表 `lc_xxx` 保持原表名不动**，避免对历史业务数据做 ALTER TABLE RENAME。

### 已与业主确认的 4 项决策

| # | 议题 | 决策 |
|---|---|---|
| 1 | 跨应用「通用资源」机制 | 保留 `appCode=common` 的内置应用 |
| 2 | 前端 URL | 带应用段：`/lowcode/app/{appCode}/...` |
| 3 | 已有数据 | 回填进内置 `default` 应用 |
| 4 | 隔离范围 | 实体/动态表、页面/组件/字典、关系/工作流/业务模块**全部隔离** |

---

## 2. 设计要点

### 2.1 命名约定

- **内置应用**：
  - `default`（承载历史业务元数据）
  - `common`（承载系统组件、系统字典、跨应用共享实体）
  - 两者 `isSystem=true`，不可删除/修改 appCode。
- **物理表名**：
  - 新建实体走 `lc_{appCode}_{entityCode}`；
  - 旧实体保留原 `lc_{entityCode}` 不动；
  - 通过 `EntityMeta.tableName` 字段统一持久化，所有 DDL/CRUD 都基于该字段，**绝不在调用处再次拼接表名**。
- **Redis 缓存键**：
  - 新：`lc:meta:{appCode}:{entityCode}:fields`
  - 旧：`lc:meta:{entityCode}:fields`（启动时一次性清除）

### 2.2 隔离与共享规则

- **业务键唯一性**：所有元数据表的单列唯一约束 drop，改为 `(app_code, biz_code)` 复合唯一。
- **资源查找**：统一封装一个 `findByAppCodeInAndXxx(List.of(currentApp, "common"), code)`，先按 `(currentApp, code)` 查，未命中回退到 `(common, code)`。
- **引用校验**：`EntityMeta` REFERENCE 字段、`EntityRelation` 两端实体——必须在 **当前 app 或 common** 中且已发布。
- **`DDLService.validateTableName`**：放宽正则到 `^lc_([a-z0-9]+_)?[a-z][a-z0-9_]*$`，兼容新旧两种命名。
- **WorkflowTask**：不冗余 `appCode`，通过 join `WorkflowInstance` 过滤；如有性能问题后续再补冗余字段。

---

## 3. Phase 1 — 后端数据模型 + 启动迁移

> 此阶段独立可合入；合入后前端零改动也能正常工作。

### 3.1 新增文件

| 路径 | 改动点 |
|---|---|
| `back/src/main/java/com/back/lowcode/entity/AppMeta.java` | 字段 `appCode`(unique 64) / `name` / `description` / `icon` / `status (active\|archived)` / `isSystem` / `createdAt` / `updatedAt`；表 `lc_app_meta`；照 `EntityMeta` 风格写 `@PrePersist`/`@PreUpdate` |
| `back/src/main/java/com/back/lowcode/repository/AppMetaRepository.java` | `findByAppCode` / `existsByAppCode` / `findByStatus` |
| `back/src/main/java/com/back/lowcode/service/AppMetaService.java` | `list` / `get` / `create` / `update` / `archive`；禁止删除/修改 `isSystem=true` 的应用 |
| `back/src/main/java/com/back/lowcode/controller/AppMetaController.java` | `@RequestMapping("/lowcode/app")` 5 端点 + `GET /lowcode/app/list/active` 给前端 store 用 |
| `back/src/main/java/com/back/lowcode/config/AppInitializer.java` | `@Component @Order(0) implements CommandLineRunner`，见 §3.3 |

### 3.2 现有实体改造（仅加 `appCode` 列 + 改唯一约束注解，**不动业务逻辑**）

| 文件 | 改动点 |
|---|---|
| `entity/EntityMeta.java` | `code` / `tableName` 去 `unique=true`；加 `appCode (length=64 nullable=false)`；`@Table(uniqueConstraints={UK(app_code,code), UK(app_code,table_name)})` |
| `entity/PageSchema.java` | 加 `appCode`，复合唯一 `(app_code, page_code)` |
| `entity/ComponentDef.java` | 加 `appCode`，复合唯一 `(app_code, comp_key)` |
| `entity/DictType.java` | 加 `appCode`，复合唯一 `(app_code, dict_code)` |
| `entity/EntityRelation.java` | 加 `appCode` |
| `entity/WorkflowInstance.java` | 加 `appCode` |
| 上述每个 Repository | 加 `findByAppCodeAndXxx(...)` 与 `findByAppCodeIn(List)`；旧的 `findByCode(...)` 保留并标 `@Deprecated`，内部实现改为 `findByAppCodeInAndCode(List.of("default","common"), code).stream().findFirst()` |
| `config/LowCodeConstants.java` | 新增 `APP_CODE_DEFAULT="default"`、`APP_CODE_COMMON="common"`、`TABLE_NAME_PATTERN="^lc_([a-z0-9]+_)?[a-z][a-z0-9_]*$"` |

### 3.3 `AppInitializer`（数据迁移核心）

执行顺序：

1. **upsert** 内置应用：`default`（isSystem=true）、`common`（isSystem=true）、`leave`（isSystem=false，业务模块）。
2. 回填 SQL（每条都带幂等 `WHERE app_code IS NULL`）：
   - `lc_entity_meta` / `lc_page_schema` / `lc_entity_relation` / `lc_workflow_instance` → `default`
   - `lc_component_def` 全部 → `common`
   - `lc_dict_type` 按 `is_system` 区分 → `common` / `default`
3. **手动 DROP 旧的单列唯一索引**（JPA `ddl-auto: update` 不会自动 drop）：
   - `SHOW INDEX FROM lc_xxx` 找出 `non_unique=0` 且单列覆盖的 index；
   - 跳过新加的复合唯一；
   - 循环 drop 旧 index。
4. 清空 Redis 旧 key：`redis-cli` 形式 `SCAN MATCH lc:meta:*:fields` + `DEL`（用 `StringRedisTemplate`）。

### 3.4 Phase 1 验证

- `./mvnw spring-boot:run` 启动无异常；
- DB `SHOW INDEX FROM lc_entity_meta` 看到 `(app_code, code)` 复合 unique，**无**单列 `code` unique；
- 现有所有接口前端零改动依然正常（旧 `findByCode` 兼容期生效）；
- `SELECT app_code, COUNT(*) FROM lc_entity_meta GROUP BY app_code` 全部为 `default`。

---

## 4. Phase 2 — Service / Controller / 业务初始化器改造

### 4.1 Controller 路径加 `/app/{appCode}` 段

| Controller | 新路径 |
|---|---|
| `EntityMetaController` | `/lowcode/app/{appCode}/entity` |
| `EntityRelationController` | `/lowcode/app/{appCode}/relation` |
| `DynamicDataController` | `/lowcode/app/{appCode}/data/{entityCode}` |
| `PageSchemaController` | `/lowcode/app/{appCode}/page` |
| `DictController` | `/lowcode/app/{appCode}/dict`；额外保留 `GET /lowcode/dict/common` 只读端点 |
| `ComponentDefController` | 保持 `/lowcode/component`（系统组件归 common，跨 app 共享） |
| `LeaveController` | `/lowcode/app/leave/leave`（硬编码 appCode） |
| `AppMetaController` | 自身保持 `/lowcode/app` |

每个方法首参增加 `@PathVariable String appCode` 并透传到 service。

### 4.2 Service 改造（统一模式）

- 所有按业务键查找改为 `findByAppCodeInAndCode(List.of(appCode, "common"), code)`，**统一封装为一个 helper**。
- **`EntityMetaService`**：
  - `publishEntity.validateReferenceFields`：引用必须在当前 app 或 common 中且已发布，否则报错；
  - `createEntity`：新实体的 `tableName = "lc_" + appCode + "_" + code`（**唯一拼接表名的点**）；
  - 缓存 key 模板带 `appCode`：`cacheFieldMeta` / `getCachedFields` / `invalidateCache` 三处同步。
- **`DynamicDataService`**：所有 `findByCode(entityCode)`（行 125/176/196/238/278/295）改为 `(appCode, common)` 聚合查找。
- **`DDLService.validateTableName`**：改为正则 `LowCodeConstants.TABLE_NAME_PATTERN`，去掉硬前缀检查。
- **`PageSchemaService` / `DictService` / `ComponentDefService` / `EntityRelationService` / `WorkflowService`**：方法签名加 `appCode`，按规则查找；`EntityRelationService` 保存时校验两端实体必须同 app 或一端 common；`WorkflowService.startWorkflow` 写入 `appCode`。
- **`LeaveService`**：硬编码 `private static final String APP_CODE = "leave";`，所有 dynamic data 调用透传该常量。

### 4.3 业务 Initializer 改造

| 文件 | 改动点 |
|---|---|
| `config/DataInitializer.java` (`@Order(1)`) | 系统组件写入 `appCode=common`；演示字典按 `isSystem` 决定 common/default |
| `config/LeaveDataInitializer.java` (`@Order(2)`) | `student/approver/leave_application` 三实体写入 `appCode=leave` |
| `config/LeavePageInitializer.java` (`@Order(3)`) | 6 个 leave 相关页面写入 `appCode=leave` |

### 4.4 Phase 2 验证

```bash
./mvnw test
./mvnw spring-boot:run
```

- `curl -X POST /lowcode/app -d '{"appCode":"demo","name":"演示"}'`
- `curl -X POST /lowcode/app/demo/entity -d '{"code":"product","name":"商品"}'`
- `curl -X POST /lowcode/app/demo/entity/{id}/publish` → 检查物理表 `lc_demo_product` 存在
- `curl -X POST /lowcode/app/demo/data/product -d '{"name":"foo"}'` → 200
- REFERENCE 字段指向 common 实体 publish 成功；指向 leave 应用实体 publish **报错**
- `redis-cli KEYS 'lc:meta:*'` → 可见 `lc:meta:demo:product:fields`

---

## 5. Phase 3 — 前端

### 5.1 新增

| 路径 | 改动点 |
|---|---|
| `front/src/api/lowcode/appMeta.ts` | `listApps` / `getApp(code)` / `createApp` / `updateApp(id, p)` / `archiveApp(id)` |
| `front/src/store/modules/app.ts` | `currentAppCode` + `apps[]`；`loadApps()` / `setCurrentApp(code)`；持久化到 localStorage |
| `front/src/pages/lowcode/app/AppList.vue` | 应用列表 + 新建/编辑/归档；点行进入 `/lowcode/app/{code}/entity` |
| `front/src/pages/lowcode/app/AppEdit.vue` | 应用表单：`appCode`（创建后不可改）/ `name` / `description` / `icon` / `status` |

### 5.2 API 包装改造（首参均加 `appCode: string`）

| 文件 | 新 URL |
|---|---|
| `api/lowcode/entityMeta.ts` | `/lowcode/app/${appCode}/entity/...` |
| `api/lowcode/pageSchema.ts` | `/lowcode/app/${appCode}/page/...` |
| `api/lowcode/dynamicData.ts` | `/lowcode/app/${appCode}/data/${entityCode}/...` |
| `api/lowcode/entityRelation.ts` | `/lowcode/app/${appCode}/relation/...` |
| `api/lowcode/dict.ts` | `/lowcode/app/${appCode}/dict/...`；新增 `listCommonDicts()` 调 `/lowcode/dict/common` |
| `api/lowcode/componentDef.ts` | 系统组件继续走 `/lowcode/component` |
| `api/lowcode/leave.ts` | URL 中 appCode 固定为 `leave` |

### 5.3 路由改造（`front/src/router/modules/lowcode.ts`）

- 顶层 `/lowcode` 重定向到 `/lowcode/apps`；
- 新增 `/lowcode/apps`(AppList) / `/lowcode/app/create` / `/lowcode/app/:appCode/edit`(AppEdit)；
- 业务子路由全部迁到 `/lowcode/app/:appCode/...`：`entity` / `entity/:id` / `entity/relations` / `data/:entityCode` / `page/list` / `page/design` / `page/view` / `page/preview/:pageCode` / `dict`；
- 路由 `props: route => ({ appCode: route.params.appCode })` 注入到页面。

### 5.4 页面改造（透传 appCode；REFERENCE/关系下拉只显示 `app ∪ common`）

需改：

- `pages/lowcode/metadata/EntityList.vue`
- `pages/lowcode/metadata/EntityEdit.vue`
- `pages/lowcode/metadata/EntityRelationList.vue`
- `pages/lowcode/metadata/DictList.vue`
- `pages/lowcode/data/DataList.vue`
- `pages/lowcode/page/PageList.vue`
- `pages/lowcode/page/PageDesigner.vue` **重点**：所有写死的跳转路径 `/lowcode/page/preview/...`、`/lowcode/data/...` 都要带 `appCode`
- `pages/lowcode/page/PageViewer.vue`
- `pages/lowcode/SchemaRenderer.vue`

顶栏布局组件加 App 切换器下拉（绑定 `store.app.currentAppCode`），切换时 `router.push('/lowcode/app/${code}/entity')`。

### 5.5 Phase 3 验证

```bash
npm run build   # 类型检查 + 打包通过
npm run dev
```

- 完整链路：应用列表 → 进入 `demo` → 实体设计 → 发布 → 数据 CRUD → 页面设计器拖组件 → 保存 → 预览；
- 切换到 `leave` 应用，请假流程仍正常。

---

## 6. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 物理表名不一致（新 `lc_{app}_{code}` vs 旧 `lc_{code}`） | 所有 DDL/CRUD 一律基于 `EntityMeta.tableName` 字段，禁止在调用处拼表名；唯一拼接点是 `EntityMetaService.createEntity` |
| JPA `ddl-auto: update` 不会 drop 旧 unique index | 必须在 `AppInitializer` 中手动 `SHOW INDEX` + `DROP INDEX`，否则插入第二个 default 之外的应用元数据会撞旧唯一约束 |
| Phase 1 后 / Phase 2 前的兼容期 | 旧 `findByCode(...)` 在 Phase 1 改为内部转发到 `(default, common)` 聚合查找，保证未改造的 service 调用方依旧能跑通 |
| 缓存污染 | `AppInitializer` 启动时清空 `lc:meta:*` 旧 key |
| WorkflowTask 未冗余 appCode | 通过 join `WorkflowInstance` 过滤；如有性能瓶颈再补冗余字段 |

---

## 7. 关键文件清单

**新建**

- `back/src/main/java/com/back/lowcode/entity/AppMeta.java`
- `back/src/main/java/com/back/lowcode/repository/AppMetaRepository.java`
- `back/src/main/java/com/back/lowcode/service/AppMetaService.java`
- `back/src/main/java/com/back/lowcode/controller/AppMetaController.java`
- `back/src/main/java/com/back/lowcode/config/AppInitializer.java`
- `front/src/api/lowcode/appMeta.ts`
- `front/src/store/modules/app.ts`
- `front/src/pages/lowcode/app/AppList.vue`
- `front/src/pages/lowcode/app/AppEdit.vue`

**重点修改**

- `back/src/main/java/com/back/lowcode/entity/{EntityMeta,PageSchema,ComponentDef,DictType,EntityRelation,WorkflowInstance}.java`
- `back/src/main/java/com/back/lowcode/repository/*.java`
- `back/src/main/java/com/back/lowcode/service/{EntityMetaService,DynamicDataService,DDLService,PageSchemaService,DictService,ComponentDefService,EntityRelationService,WorkflowService,LeaveService}.java`
- `back/src/main/java/com/back/lowcode/controller/*.java`
- `back/src/main/java/com/back/lowcode/config/{LowCodeConstants,DataInitializer,LeaveDataInitializer,LeavePageInitializer}.java`
- `front/src/router/modules/lowcode.ts`
- `front/src/api/lowcode/*.ts`
- `front/src/pages/lowcode/**`（按 §5.4 列表）

---

## 8. 实施顺序

```
Phase 1（数据模型 + 启动迁移） → 部署验证 → Phase 2（Service/Controller） → 部署验证 → Phase 3（前端）
```

每个 Phase 独立可合入；Phase 1 完成后前端零改动可继续工作（旧路径 + 旧 finder 兼容）；Phase 2 完成后通过 curl 可完整验证后端能力；Phase 3 完成后整体链路打通。
