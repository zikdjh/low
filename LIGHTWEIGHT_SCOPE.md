# 轻量级低代码平台 · 范围定义

> 文档版本：2026-06-15 · 适用分支：`develop` / `feature/lowcode-phase-1-entity-metadata`
> 本文档是 `DEVELOPMENT.md` 的**范围补丁**：明确这个项目要做什么、不做什么、做到什么程度就停手。
> 阅读顺序：`CLAUDE.md`（代码现状）→ `DEVELOPMENT.md`（路线图）→ 本文档（范围裁剪）→ `FEATURES.md`（具体怎么做）

---

## 0. 产品定位（一句话）

**对标 Retool / Appsmith / Budibase 的开源轻量替代** — 给团队内部用的"业务系统脚手架"，目标是"3 天上手、1 周做完一个内部工具"。

**不是**：钉钉宜搭、简道云、明道云、OutSystems、Mendix 这种带工作流 / 流程引擎 / 多租户 SaaS 的中重型平台。

---

## 1. 明确放弃的功能（不做清单）

把范围先砍下来，避免技术债越滚越大。下表中的功能**未来也不做**，需要时引入第三方系统替代。

| 砍掉的能力 | 替代方案 | 理由 |
|----------|---------|------|
| ❌ 工作流 / 审批引擎 | 外接钉钉 / 飞书审批 | 工作流引擎本身就是另一个产品 |
| ❌ BPMN 服务编排 | n8n / Zapier / 手写后端 | 同上，重资产 |
| ❌ 多租户 SaaS | 一套部署一个团队 | 加 `tenant_id` 是技术债 |
| ❌ 自定义组件市场（远程加载 JS）| 改源码加 `*Element.vue` | 安全风险高，复杂度大 |
| ❌ 实体多语言字段 | 业务字段中文为主 | 内部工具不需要 |
| ❌ 主题市场 / 换肤 | 一套默认 TDesign 主题 | 内部工具不需要 |
| ❌ 移动端响应式 | PC 优先 | 内部工具 PC 用 |
| ❌ 表达式引擎（`{{form.x * 1.13}}`）| 事件 DSL + 后端 webhook | 表达式安全模型复杂 |
| ❌ 脚本 hook / 服务端 JS | 后端 webhook 扩展 | 沙箱难做 |
| ❌ 实时多人协作编辑 | 乐观锁防覆盖 | 协作算法太重 |
| ❌ 自定义 SQL 查询视图 | 多建几个实体 | 引入即破坏 DDLService 单一出口 |
| ❌ 复杂索引（联合 / 全文） | 手动 ALTER 即可 | 不影响 80% 场景 |
| ❌ 行级权限（数据所有人） | 业务侧自己加 owner 字段 | 一旦做了拉不下来 |
| ❌ 字段级脱敏 | 业务侧自己处理 | 同上 |

**核心原则**：

1. 能用约定代替配置就用约定（如 `code: 1` 成功）。
2. 能让用户写后端代码扩展就不做"低代码内的高代码"（如不做 JS 沙箱）。
3. 不为 5% 的场景增加 50% 的代码（如不做多租户）。

---

## 2. 要做的功能（按优先级）

### 2.1 P0 红线（不做就裸奔）

这些功能**没做完不算 1.0**，安全和稳定性的硬底线。

| # | 任务 | 现状 | 改动文件 |
|---|------|------|---------|
| P0-1 | **JWT 鉴权拦截器** | 🔴 `PathInterceptor` 只打日志 | 新建 `JwtInterceptor`, 注册到 `WebConfig` |
| P0-2 | **SQL 注入修复** | 🔴 `DynamicDataService` 直接拼接表名/列名 | 重构 `listData`/`getById`/`update` 用反引号 + 白名单 |
| P0-3 | **基础 RBAC**（用户/角色/实体级权限）| 🔴 完全没有 | 新建 `User`/`Role`/`EntityAcl` 三张表 + 拦截器 |
| P0-4 | DB 密码移出仓库 | 🔴 `application.yaml` 明文 | `application-local.yaml` + `.gitignore` |
| P0-5 | DDLService 单元测试 | 🔴 0 测试 | `DDLServiceTest`：保留字 / 非法前缀 / SQL snapshot |

**完成定义**：可以给同事开账号，不担心误删、越权、注入。

### 2.2 P1 可用性（决定"低代码"名副其实）

这些做完，"拖三个组件就是一个能用页面"的承诺才能兑现。

| # | 任务 | 现状 | 改动文件 |
|---|------|------|---------|
| P1-1 | **REFERENCE 字段（外键）** | 🔴 缺失 | `FieldType` 加 REFERENCE，`SelectElement` 支持按实体取值 |
| P1-2 | **SchemaRenderer 改递归** | 🟡 硬编码 3 层 | 改递归组件，支持任意深度 |
| P1-3 | **运行时入口 `/run/:pageCode`** | 🔴 缺失 | 新建路由 + 页面，从后端拉 PageSchema 渲染 |
| P1-4 | **TableElement 接通后端** | 🟡 仅 mock | props 读 entityCode → 调 `dynamicDataApi.list` |
| P1-5 | **FormElement 接通后端** | 🟡 仅 mock | 自动按 FieldMeta 渲染表单 + 提交 |
| P1-6 | **SelectElement 接通字典** | 🟡 仅 mock | dictCode 不空时调 `dictApi.getItems` |
| P1-7 | **事件系统（5 动作 DSL）** | 🟡 仅类型定义 | navigate/callApi/refresh/setValue/showMessage |
| P1-8 | **页面参数**（`?id=123` 注入到组件 props） | 🔴 缺失 | URL → SchemaRenderer context |
| P1-9 | **默认值类型校验** | 🟡 缺失 | `FieldType.validateDefault(String)` |
| P1-10 | **唯一约束（单字段 unique）** | 🔴 缺失 | `FieldMeta.uniqueFlag` + DDL 加 `UNIQUE KEY` |
| P1-11 | **字段类型变更 → MODIFY COLUMN** | 🔴 不支持 | `DDLService.generateAlterTable` 识别同 code 类型变化 |
| P1-12 | **EntityListRequest 真正生效**（搜索/状态过滤）| 🟡 后端忽略 | `EntityMetaRepository` 加 spec 查询 |

**完成定义**：拖 Table + Button + Form 三个组件，配上 entityCode，不写代码就有一个能用的"客户管理"页面。

### 2.3 P2 体验打磨（可日常用）

| # | 任务 | 现状 |
|---|------|------|
| P2-1 | **Excel 导入 / 导出**（数据） | 🔴 缺失 |
| P2-2 | **文件上传组件** + 后端文件存储（本地即可） | 🔴 缺失 |
| P2-3 | **Tabs / 折叠面板** 容器组件 | 🔴 缺失 |
| P2-4 | **多条件高级搜索**（AND 组合 + 范围） | 🟡 仅 LIKE |
| P2-5 | **批量编辑** | 🔴 缺失 |
| P2-6 | **复制 / 粘贴组件**（设计器） | 🔴 缺失 |
| P2-7 | **操作日志**（业务数据 CRUD 审计） | 🔴 缺失 |
| P2-8 | **DDL 回滚 UI**（基于 `lc_ddl_log`） | 🟡 有日志无 UI |
| P2-9 | **乐观锁**（`PageSchema.version` 启用 `@Version`） | 🟡 有列未启用 |
| P2-10 | **Docker Compose 一键启动** | 🔴 缺失 |
| P2-11 | **应用导出 zip**（实体+页面+字典 跨环境迁移） | 🔴 缺失 |

**完成定义**：每周用 5+ 次也不嫌烦。

### 2.4 P3 可选（有空再做）

- 富文本编辑器组件
- 字段联动（A 字段值变 → B 字段显示/隐藏）
- 实体生命周期 webhook（创建客户 → POST 到 OA）
- Dashboard 多 Tab 聚合查询页

---

## 3. 落地路线（3 个月节奏）

### Phase 2：稳健化（**4 周**）

```
Week 1     P0-1  JWT 拦截器 + 用户/角色/AccessControl 三表
Week 2     P0-3  实体级 RBAC（view/edit/delete）拦截
Week 3     P0-2  DynamicDataService SQL 注入修复 + 测试
Week 4     P0-4 配置外置 + P0-5 DDLService 测试 + P1-9 默认值校验
```

**里程碑 M1**：可对外发账号，红线全部解决。

### Phase 3：可用化（**4 周**）

```
Week 5     P1-1 REFERENCE 字段 + P1-10 唯一约束
Week 6     P1-2 递归渲染 + P1-3 运行时入口 + P1-8 页面参数
Week 7     P1-4/5/6 三个 Element 接通后端 + P1-7 事件 DSL
Week 8     P1-11 字段类型变更 + P1-12 列表查询参数
```

**里程碑 M2**：「不写代码做一个内部工具」承诺兑现。

### Phase 4：体验打磨（**2-4 周**）

```
Week 9-10  P2-1 Excel 导入导出 + P2-2 文件上传 + P2-3 Tabs
Week 11    P2-4 高级搜索 + P2-5 批量编辑 + P2-6 复制粘贴
Week 12    P2-7 操作日志 + P2-10 Docker + P2-11 应用导出
```

**里程碑 M3**：日常可用，能交付给业务团队。

> 之后 P3 按需求顺序排，不再设固定节奏。

---

## 4. 能力地图（最终目标态）

```
┌──────────────────────────────────────────────────┐
│ 核心能力（做扎实）                                │
│   数据建模  ████████░░  80% → 95%（补 REFERENCE）│
│   CRUD     ███████░░░  70% → 90%（补导入导出）   │
│   页面设计 ██████░░░░  60% → 90%（递归 + 运行时）│
│   组件库   █████░░░░░  50% → 75%（够用就行）     │
│   事件 DSL █░░░░░░░░░  10% → 70%（5 个动作）     │
├──────────────────────────────────────────────────┤
│ 安全（红线）                                      │
│   鉴权     ░░░░░░░░░░   0% → 100% (P0)           │
│   RBAC     ░░░░░░░░░░   0% → 70%（实体级即可）   │
│   SQL 防护 █░░░░░░░░░  10% → 100% (P0)           │
│   审计     ██░░░░░░░░  20% → 60%（操作日志）     │
├──────────────────────────────────────────────────┤
│ 明确不做                                          │
│   工作流、表达式引擎、多租户、组件市场、移动端、 │
│   多语言、行级权限、字段脱敏、自定义 SQL 视图    │
└──────────────────────────────────────────────────┘
```

**对标参考**：

- ✅ 最接近：**Retool / Appsmith / Budibase**（"表格 + 表单 + 按钮 + 简单事件"为核心）
- ❌ 不对标：钉钉宜搭、简道云、明道云、OutSystems、Mendix（流程引擎是核心壁垒）

---

## 5. 范围管控原则

未来收到"加个 X 功能"的需求时，按下表判断：

| 信号 | 处理 |
|------|------|
| 第 1 节"不做清单"里 | 直接拒绝，引用本文档 |
| 不在不做清单，但和 P0/P1/P2 路线冲突 | 排到 P3 之后再说 |
| 5% 用户的需求 + 50% 代码量 | 拒绝，让用户写后端 webhook |
| 影响"两个数据平面分离"原则 | 拒绝，找别的实现方式 |
| 影响"DDLService 单一 DDL 出口"原则 | 拒绝 |
| 一个组件能解决，但要求"自定义" | 拒绝，让用户改源码 PR |

> 凡是会让"对标 Retool"变成"对标宜搭"的需求，一律拒绝。

---

## 6. 与其它文档的关系

```
CLAUDE.md             ← 代码硬约束（永远不变的工程纪律）
DEVELOPMENT.md        ← 完整路线图（Phase 1 总结 + 后续展望）
LIGHTWEIGHT_SCOPE.md  ← 本文档（范围裁剪 = 砍掉 + 留下）
FEATURES.md           ← 功能开发手册（具体怎么改文件、踩什么坑）
```

冲突时优先级：`CLAUDE.md` > `LIGHTWEIGHT_SCOPE.md` > `DEVELOPMENT.md` > `FEATURES.md`。

`DEVELOPMENT.md` 中"Phase 4/5"的远景部分，凡与本文档第 1 节冲突的，以本文档为准 — 那些远景是写给"如果有一天我们改主意做中重型"的备忘，不是当前的执行项。
