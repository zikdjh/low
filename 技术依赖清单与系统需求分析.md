# 低代码平台 — 技术依赖清单 & 系统需求反向推理

> 生成日期：2026-06-25  
> 项目分支：feature/lowcode-phase-2-code-generator

---

## 一、后端技术依赖清单（Java 17 / Spring Boot 3.5.14）

| 类别 | 依赖 | 版本 | 用途 |
|------|------|------|------|
| **核心框架** | spring-boot-starter-web | 3.5.14 | RESTful API 服务（内嵌 Tomcat） |
| **持久层-ORM** | spring-boot-starter-data-jpa | 3.5.14 | JPA + Hibernate，管理固定元数据表 |
| **持久层-JDBC** | spring-boot-starter-jdbc | 3.5.14 | JdbcTemplate / NamedParameterJdbcTemplate 操作动态用户数据表 |
| **数据库驱动** | mysql-connector-j | (managed) | MySQL 8.x JDBC 驱动 |
| **缓存** | spring-boot-starter-data-redis | 3.5.14 | Redis 缓存字段元数据（TTL 30天） |
| **参数校验** | spring-boot-starter-validation | 3.5.14 | DTO 入参 JSR-380 Bean Validation |
| **安全框架** | spring-boot-starter-security | 3.5.14 | 认证鉴权、CORS、CSRF 禁用、无状态会话 |
| **JWT** | java-jwt (com.auth0) | 4.5.1 | Access Token（45min）+ Refresh Token（7天 Cookie） |
| **模板引擎** | freemarker | 2.3.34 | 代码生成器：8 个 FTL 模板渲染 Java/Vue 代码骨架 |
| **代码简化** | lombok | (managed) | @Data / @Builder / @RequiredArgsConstructor 等注解处理器 |
| **测试** | spring-boot-starter-test | 3.5.14 | JUnit 5 + Mockito 测试框架 |
| **构建** | maven-surefire-plugin | — | 测试执行 |
| **构建** | spring-boot-maven-plugin | — | Fat JAR 打包（排除 Lombok） |
| **构建** | maven-compiler-plugin | — | Lombok 注解处理器路径配置 |

### 基础设施依赖

| 组件 | 版本/说明 | 用途 |
|------|-----------|------|
| **MySQL** | 8.x (`127.0.0.1:3306` / `low_end` 库) | 存储元数据表（`lc_*`）+ 用户动态业务表（`lc_*`） |
| **Redis** | (`localhost:6379`) | 缓存已发布实体的字段元数据 |
| **Java** | 17 | 运行环境 |
| **Maven Wrapper** | `./mvnw` | 免安装 Maven 构建 |

---

## 二、前端技术依赖清单（Vue 3 + TypeScript + Vite）

### 运行时依赖

| 类别 | 依赖 | 版本 | 用途 |
|------|------|------|------|
| **UI 框架** | vue | 3.5.34 | Composition API 响应式框架 |
| **路由** | vue-router | 5.1.0 | SPA 路由（含懒加载） |
| **状态管理** | pinia | 3.0.4 | 全局状态管理（designer / user / setting / cropper / markdownEditor） |
| **状态持久化** | pinia-plugin-persistedstate | 4.7.1 | Pinia 状态自动持久化到 localStorage |
| **HTTP 客户端** | axios | 1.16.1 | API 请求 + 拦截器（token 注入 / 401 刷新） |
| **UI 组件库** | tdesign-vue-next | 1.20.0 | 腾讯 TDesign 企业级组件库 |
| **图标库** | tdesign-icons-vue-next | 0.4.4 | TDesign 配套图标集 |
| **AI 聊天** | @tdesign-vue-next/chat | 0.5.2 | AI 助手聊天 UI 组件 |
| **图表** | echarts | 6.1.0 | 页面设计器中图表组件渲染 |
| **流程图** | mermaid | 11.15.0 | Markdown 中流程图/时序图渲染 |
| **代码高亮** | highlight.js | 11.11.1 | 代码块语法高亮 |
| **数学公式** | katex | 0.17.0 | Markdown 中数学公式渲染 |
| **Markdown 编辑器** | md-editor-v3 | 6.5.1 | 富文本/Markdown 编辑 |
| **工具库** | lodash | 4.18.1 | 通用工具函数 |
| **全屏** | screenfull | 6.0.2 | 全屏 API 封装 |
| **主题色** | tvision-color | 1.6.0 | TDesign 主题色生成算法 |
| **格式化** | prettier | 3.8.3 | 代码格式化 |
| **图片裁剪** | vue-cropper | 1.1.4 | 图片裁剪组件 |
| **语音输入** | @speechmatics/browser-audio-input | 2.0.3 | 浏览器音频采集 |
| **Markdown 导入** | vite-plugin-markdown | 2.2.0 | .md 文件直接 import 为字符串 |

### 开发依赖

| 类别 | 依赖 | 版本 | 用途 |
|------|------|------|------|
| **构建工具** | vite | 8.0.14 | 开发服务器 + 打包 |
| **语言** | typescript | ~6.0.3 | 类型检查 |
| **Vue 编译** | @vitejs/plugin-vue | 6.0.7 | Vite Vue SFC 编译 |
| **类型检查** | vue-tsc | 3.3.1 | Vue 模板类型检查 |
| **CSS 预处理** | less / less-loader | 4.6.4 / 13.0.0 | Less 样式 |
| **CSS 后处理** | postcss + autoprefixer | 8.5.10 / 10.5.0 | CSS 自动前缀 |
| **E2E 测试** | playwright | 1.61.0 | 浏览器自动化测试 |
| **Markdown 导入** | @vavt/vite-plugin-import-markdown | 1.0.1 | Vite 中导入 Markdown |
| **类型声明** | @types/katex / @types/node | — | TypeScript 类型补充 |

---

## 三、反向推理：系统需求分析

基于以上技术选型和代码架构，可反向推理出以下 12 大需求域：

---

### 🧩 需求域 1：低代码元数据驱动架构

**需求推导依据**：`EntityMeta` + `FieldMeta` 两表驱动；`FieldType` 枚举桥接元数据到 SQL；`DDLService` 动态建表/改表。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-META-01** | 管理员可在后台定义业务实体（名称、编码、描述），系统自动生成对应的 MySQL 物理表 |
| **REQ-META-02** | 支持 11 种字段类型：VARCHAR、INTEGER、LONG、DOUBLE、BOOLEAN、DATE、DATETIME、TEXT、JSON、DECIMAL、REFERENCE |
| **REQ-META-03** | 实体具有完整生命周期：草稿(draft) → 已发布(published) → 已归档(archived) |
| **REQ-META-04** | 已发布实体修改字段时，采用增量 DDL（ADD COLUMN + 软删除重命名 `__deleted_*`），绝不执行 DROP |
| **REQ-META-05** | 实体编码和字段编码必须通过安全性校验（字母开头、合法字符、非 MySQL 保留字） |
| **REQ-META-06** | 所有 DDL 操作记录至审计日志表 `lc_ddl_log`，记录成功/失败状态 |
| **REQ-META-07** | 字段元数据缓存至 Redis（TTL 30 天），动态数据查询时从缓存读取最新 schema |

---

### 🔄 需求域 2：动态数据 CRUD

**需求推导依据**：`DynamicDataService` 基于 `JdbcTemplate` 动态构建 SQL；`DynamicDataController` 仅接受已发布实体的 CRUD。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-DATA-01** | 对已发布实体提供标准 RESTful CRUD（GET 分页列表、GET 详情、POST 新增、PUT 修改、DELETE 删除） |
| **REQ-DATA-02** | 支持动态筛选：按字段值精确/模糊查询、多字段组合、日期范围查询 |
| **REQ-DATA-03** | 支持动态排序（多字段、升降序）和分页（page + pageSize） |
| **REQ-DATA-04** | 查询时只返回未软删除的字段（排除 `__deleted_*` 列） |
| **REQ-DATA-05** | 状态守卫：草稿/归档状态的实体拒绝 CRUD 操作 |

---

### 🎨 需求域 3：可视化页面设计器

**需求推导依据**：`PageSchema`（`layout_json` LONGTEXT）+ `PageDesigner.vue` + Pinia `designer.ts`（拖拽、选中、撤销历史）。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-PAGE-01** | 提供拖拽式可视化页面设计器，左侧组件面板 + 中间画布 + 右侧属性编辑器三栏布局 |
| **REQ-PAGE-02** | 支持 25+ 种页面组件：Button、Input、Select、Date、Table、Chart、Form、Tabs、Steps、Upload 等 |
| **REQ-PAGE-03** | 组件属性可视化编辑（标签、占位符、必填、禁用、字典绑定等） |
| **REQ-PAGE-04** | 页面布局保存为 JSON Schema（`layout_json`），支持版本管理 |
| **REQ-PAGE-05** | 提供独立的运行时渲染器（`SchemaRenderer.vue`），根据 JSON Schema 动态渲染页面 |
| **REQ-PAGE-06** | 支持撤销/重做操作历史 |
| **REQ-PAGE-07** | 页面与实体绑定（`entity_code`），实现数据源关联 |

---

### 🏗️ 需求域 4：代码生成器

**需求推导依据**：`CodeGenService` + 8 个 FreeMarker 模板（Entity/DTO/Repository/Service/Controller + api.ts/List.vue/Edit.vue）。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-CODEGEN-01** | 根据已定义实体自动生成完整后端代码：JPA Entity、DTO、Repository、Service、Controller |
| **REQ-CODEGEN-02** | 自动生成前端代码：TypeScript API 封装 + 列表页 + 编辑页（Vue SFC） |
| **REQ-CODEGEN-03** | 支持代码预览（在线查看生成文件内容）和一键 ZIP 下载 |
| **REQ-CODEGEN-04** | 生成代码遵循项目分层规范（Controller → Service → Repository → Entity） |
| **REQ-CODEGEN-05** | 根据字段类型自动映射 Java 类型、TypeScript 类型、表单校验规则 |

---

### 🔐 需求域 5：认证与权限

**需求推导依据**：Spring Security + JWT（java-jwt）+ `SecurityConfig` + User/Role/Permission 实体。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-AUTH-01** | 支持用户注册、登录（JWT Access Token 45min + Refresh Token 7天 HttpOnly Cookie） |
| **REQ-AUTH-02** | 基于 RBAC 的权限模型：用户 → 角色 → 权限 |
| **REQ-AUTH-03** | 管理员端（`/admin/**`）仅 admin/root 角色可访问 |
| **REQ-AUTH-04** | 低代码平台管理接口（实体/页面/字典）当前全部放行，预留认证接入点 |
| **REQ-AUTH-05** | 无状态会话（STATELESS），完全由 JWT 承载认证信息 |
| **REQ-AUTH-06** | 支持密码修改功能 |
| **REQ-AUTH-07** | 前端 axios 拦截器自动注入 token，401 时自动尝试 refresh |

---

### 📋 需求域 6：工作流引擎（请假审批）

**需求推导依据**：`WorkflowService` + `WorkflowInstance` / `WorkflowTask` 实体 + `LeaveController`。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-WF-01** | 支持请假申请提交，根据请假天数动态决定审批节点（≤3天辅导员审批，>3天辅导员+系主任审批） |
| **REQ-WF-02** | 审批流程：发起 → 节点审批（通过/驳回）→ 流转下一节点或完成 |
| **REQ-WF-03** | 支持按角色分配审批任务（counselor / dept_head） |
| **REQ-WF-04** | 发起人可撤回待审批申请 |
| **REQ-WF-05** | 提供流程进度查询（当前节点、各节点状态、审批意见） |

---

### 📦 需求域 7：业务应用管理

**需求推导依据**：`BusinessApp` 实体 + `BusinessAppController` + `BusinessAppInitializer`。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-APP-01** | 支持将实体和页面按"业务应用"分组管理 |
| **REQ-APP-02** | 应用下可关联多个实体和页面 |
| **REQ-APP-03** | 系统启动时自动初始化默认业务应用 |

---

### 📖 需求域 8：字典管理

**需求推导依据**：`DictType` / `DictItem` + `DictController` + `DataInitializer` 种子数据。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-DICT-01** | 支持字典类型定义（如 status、gender、yes_no）和字典项管理 |
| **REQ-DICT-02** | 系统首次启动自动种子化 3 个默认字典 |
| **REQ-DICT-03** | 字段可绑定字典，前端表单自动渲染为下拉选择 |

---

### 🔗 需求域 9：实体关系管理

**需求推导依据**：`EntityRelation` + `EntityRelationController` + `FieldType.REFERENCE`。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-REL-01** | 支持定义实体间关系（一对一、一对多、多对多） |
| **REQ-REL-02** | REFERENCE 字段类型支持外键级联关系 |

---

### 🔔 需求域 10：通知系统

**需求推导依据**：`Notification` 实体 + `NotificationController` + `NotificationService`。

| 需求编号 | 需求描述 |
|----------|----------|
| **REQ-NOTIF-01** | 支持系统通知的创建、发布、查询 |
| **REQ-NOTIF-02** | 已发布通知公开可见，其他管理接口需 admin/root 角色 |
| **REQ-NOTIF-03** | 前端提供通知中心页面 |
---

## 四、需求域映射总图

```
┌─────────────────────────────────────────────────────────────────┐
│                      低代码平台系统需求全景                         │
├────────────┬────────────────────────────────────────────────────┤
│ REQ-META   │ 元数据驱动架构 — 动态实体定义 / DDL 生成 / 字段缓存     │
│ REQ-DATA   │ 动态数据 CRUD — 无 JPA 实体的通用增删改查             │
│ REQ-PAGE   │ 可视化页面设计器 — 拖拽搭建 / JSON Schema 渲染         │
│ REQ-CODEGEN│ 代码生成器 — FreeMarker 模板 → Java + Vue 代码        │
│ REQ-AUTH   │ 认证鉴权 — JWT + RBAC + Spring Security             │
│ REQ-WF     │ 工作流引擎 — 请假审批（辅导员→系主任）                  │
│ REQ-APP    │ 业务应用管理 — 实体/页面分组                           │
│ REQ-DICT   │ 字典管理 — 数据字典 + 字段绑定                         │
│ REQ-REL    │ 实体关系管理 — 外键引用                                │
│ REQ-NOTIF  │ 通知系统 — 消息公告                                   │
│ REQ-AI     │ AI 助手 — 语音/文本对话                               │
│ REQ-PLAT   │ 通用平台能力 — 文件上传/主题/Markdown/图表/测试         │
└────────────┴────────────────────────────────────────────────────┘
```

---

## 五、关键技术决策（从选型反向推理）

| 决策点 | 推断理由 |
|--------|----------|
| **双数据平面架构** | JPA 管理固定元数据表 + JDBC 操作动态用户表 — 因为用户数据表在运行时创建，无法预编译 JPA 实体类 |
| **Redis 缓存字段 schema** | 动态 CRUD 每次需查询 `FieldMeta` 确定列集合，Redis 缓存避免高频 DB 查询 |
| **FreeMarker 而非代码 AST 生成** | 模板引擎渲染代码骨架，简单可控，类似 JeecgBoot 方案 |
| **STATELESS + JWT** | 前后端分离 + 未来可水平扩展；HttpOnly Cookie 存 refresh token 防 XSS |
| **TDesign 而非 Element Plus** | 腾讯生态企业级场景，组件丰富度、主题定制能力匹配低代码平台需求 |
| **Vite 8 + TypeScript 6 + Vue 3.5** | 全部采用最新大版本，享受最新性能优化和类型安全 |
| **DDL 软删除而非 DROP COLUMN** | 数据安全策略 — 删除字段不丢失已有数据，人工确认后再清理 |
