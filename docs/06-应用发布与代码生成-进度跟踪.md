# 低代码平台 Phase 2 进度跟踪：应用发布与挂载 + 代码自动生成

> 分支：`feature/lowcode-phase-2-release-codegen`（从 `feature/lowcode-phase-1-entity-metadata` 派生）
> 改造方案：见 [`05-应用发布与代码生成-改造方案.md`](./05-应用发布与代码生成-改造方案.md)
> 起始日期：2026-06-24
> 状态图例： ⬜ 未开始　🟡 进行中　✅ 已完成　⛔ 阻塞　⏸ 暂缓

---

## 总览

| 里程碑 | 进度 | 状态 | 开始 | 计划完成 | 实际完成 |
|---|---|---|---|---|---|
| M1 发布管线与快照 | 18 / 18 | ✅ | 2026-06-24 | - | 2026-06-24 |
| M2 菜单与路由挂载 | 6 / 12 | 🟡 | 2026-06-24 | - | - |
| M3 前后端代码生成 | 0 / 16 | ⬜ | - | - | - |
| M4 请假应用迁移 | 0 / 7 | ⬜ | - | - | - |
| **合计** | **24 / 53** | **🟡** | 2026-06-24 | - | - |

---

## M1 发布管线与快照（后端为主）

### M1.1 数据层

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M1-01 | 新增 `lc_release` 表（含 schema.sql） | ✅ | - | JPA `ddl-auto=update` 自动建表，无需独立 schema.sql |
| M1-02 | 新增 `lc_release_item` 表 | ✅ | - | LONGTEXT snapshot_json + 复合索引 |
| M1-03 | 新增 `lc_release_log` 表 | ✅ | - | 五阶段审计 |
| M1-04 | 实体类 `Release / ReleaseItem / ReleaseLog` | ✅ | - | 放 `lowcode.entity` 与其他实体同包 |
| M1-05 | Repository 三件 | ✅ | - | JPA Repository |

### M1.2 服务层

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M1-06 | `ValidatePhase`：layoutJson + 字段闭环 + 命名冲突校验 | ✅ | - | 递归扫描 layoutJson 提取 compKey 校验 |
| M1-07 | `SnapshotPhase`：entity / page / component / dict / menu 序列化 | ✅ | - | sha256 per item + 汇总 checksum；menu 待 M2 补 |
| M1-08 | `GeneratePhase`：调 M3 代码生成器接口（先留空 stub） | ✅ | - | M1 阶段空实现，M3 接入 |
| M1-09 | `MountPhase`：写 `lc_app_menu(release_id=新)` | ✅ | - | M1 阶段空实现，M2 接入 |
| M1-10 | `ActivatePhase`：原子切换 active release | ✅ | - | 旧 active 自动标 rolledback |
| M1-11 | `ReleaseService` 编排 + 失败回滚 | ✅ | - | self-reference + REQUIRES_NEW 保证失败标记写入 |

### M1.3 接口层

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M1-12 | `POST /lowcode/release/{appCode}` 发起发布 | ✅ | - | body: PublishRequest{versionBump, notes, createdBy, activate} |
| M1-13 | `GET  /lowcode/release/{appCode}` 历史列表 | ✅ | - | 按 createdAt 倒序 |
| M1-14 | `GET  /lowcode/release/detail/{id}` 单次详情 + 阶段日志 | ✅ | - | 路径调整为 `/detail/{id}` 避免与 `/{appCode}` 冲突 |
| M1-15 | `GET  /lowcode/release/{id}/items` 快照清单 | ✅ | - | - |
| M1-16 | `POST /lowcode/release/{id}/rollback` 回滚 | ✅ | - | 仅 rolledback/draft 状态可回滚 |
| M1-17 | `POST /lowcode/release/{id}/activate` 灰度激活 | ✅ | - | 仅 draft 状态可激活 |

### M1.4 Phase 1 兼容

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M1-18 | 弃用 `PageSchemaController.publish/unpublish`，改由 ReleaseService 反向维护 status | ✅ | - | 加 @Deprecated，保留过渡期 |

---

## M2 菜单与路由挂载

### M2.1 后端

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M2-01 | 新增 `lc_app_menu` 表 | ✅ | - | 通过 `Menu` 实体由 JPA `ddl-auto=update` 自动建表，含 release_id + parent_id 自引用 + 复合索引 |
| M2-02 | `Menu` 实体 + Repository | ✅ | - | 同包 `lowcode.entity`；Repository 提供 findDraftMenu / findReleaseMenu / 批量删除 |
| M2-03 | `MenuService`：草稿态 CRUD + 拖拽排序 | ✅ | - | 草稿态 `release_id IS NULL`；saveDraft 递归落库回填 id；reorder 单事务批量更新；cloneDraftToRelease 供 MountPhase 调用 |
| M2-04 | `GET /lowcode/app/{appCode}/menu`（active release） | ✅ | - | 终端用户读 active release 菜单树 |
| M2-05 | `GET/PUT /lowcode/app/{appCode}/menu/draft` | ✅ | - | 设计器读写草稿 |
| M2-06 | `POST /lowcode/app/{appCode}/menu/reorder` | ✅ | - | 拖拽局部排序；多更新一个事务原子提交 |

### M2.2 前端

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M2-07 | 抽出公共 `front/src/components/lowcode/SchemaRenderer.vue` | ⬜ | - | 收敛 32 项 componentMap |
| M2-08 | `AppRuntime.vue` 改读菜单树，多级展开 + 图标 | ⬜ | - | - |
| M2-09 | `runtime.ts` 路由：`/run/:appCode/:pageCode?` 深链 | ⬜ | - | - |
| M2-10 | 删 `PageViewer.vue` 的 `/leave/` 硬编码，监听 `params.pageCode` | ⬜ | - | 验收硬指标 |
| M2-11 | 新增 `AppMenuEditor.vue` 树形拖拽编辑器 | ⬜ | - | 接菜单草稿 API |
| M2-12 | `BusinessAppPage.vue` 加「编辑菜单」入口 | ⬜ | - | - |

---

## M3 前后端代码自动生成

### M3.1 基础设施

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M3-01 | pom 引入 `org.freemarker:freemarker` | ⬜ | - | - |
| M3-02 | 模板目录 `codegen/templates/` 初始化 | ⬜ | - | - |
| M3-03 | 字段类型映射矩阵 + 单测 | ⬜ | - | EntityMeta → Java/SQL/TS 类型 |
| M3-04 | `CodegenContext` 模板上下文构建器 | ⬜ | - | - |

### M3.2 后端模板

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M3-05 | `entity.java.ftl` JPA 实体 + 外键 | ⬜ | - | - |
| M3-06 | `repository.java.ftl` | ⬜ | - | - |
| M3-07 | `service.java.ftl` CRUD + 校验 + 分页 | ⬜ | - | - |
| M3-08 | `controller.java.ftl` REST 端点 | ⬜ | - | - |
| M3-09 | `dto.java.ftl` + `query.java.ftl` | ⬜ | - | - |
| M3-10 | `ddl.sql.ftl` 整应用 DDL | ⬜ | - | - |

### M3.3 前端模板

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M3-11 | `list-page.vue.ftl` / `form-page.vue.ftl` / `detail-page.vue.ftl` | ⬜ | - | - |
| M3-12 | `api.ts.ftl` axios 接口 | ⬜ | - | - |
| M3-13 | `router.ts.ftl` + `menu.ts.ftl` | ⬜ | - | - |

### M3.4 接口与 UI

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M3-14 | `POST /lowcode/codegen/preview/{releaseId}` 内存预览 | ⬜ | - | - |
| M3-15 | `POST /lowcode/codegen/build/{releaseId}` 落 target | ⬜ | - | - |
| M3-16 | `GET  /lowcode/codegen/download/{releaseId}` zip 下载 | ⬜ | - | Monaco 预览 |

---

## M4 请假应用迁移（自举验证）

| # | 任务 | 状态 | 负责人 | 备注 |
|---|---|---|---|---|
| M4-01 | 导出 `seed/leave_management-v1.0.0.json` | ⬜ | - | 应用级 release 快照 |
| M4-02 | `SeedReleaseLoader` 启动时灌入 release | ⬜ | - | 仅当无 active 时执行 |
| M4-03 | 删 `LeavePageInitializer / LeavePageInitializerV2` | ⬜ | - | - |
| M4-04 | 删 `LeaveDataInitializer` 中的页面 / 菜单部分 | ⬜ | - | 保留种子业务数据 |
| M4-05 | 删 `BusinessAppInitializer` | ⬜ | - | - |
| M4-06 | 收缩 `front/src/router/modules/leave.ts` | ⬜ | - | 仅留管理端 |
| M4-07 | 全量回归：学生 / 教师 / 辅导员 / 管理员四端走通 | ⬜ | - | 走 `/run/leave_management/*` |

---

## 验收 Checklist

完成 Phase 2 前需逐项打勾：

- [ ] EntityMeta / PageSchema 修改不影响线上，必须经 release 上线
- [ ] 任意历史 release 一键回滚，5 秒内对终端用户生效
- [ ] 业务应用菜单可在管理端拖拽配置
- [ ] 任意 release 可下载完整工程 zip（Spring Boot + Vue），IDE 内可编译运行
- [ ] 项目无 `leave_` 前缀的页面 / 菜单硬编码
- [ ] `AppRuntime.vue` 与 `PageViewer.vue` 共用唯一 `SchemaRenderer`
- [ ] 发布流水线审计可在 `/lowcode/release/{id}` 查看阶段日志

---

## 风险与阻塞登记

| 日期 | 项 | 影响里程碑 | 描述 | 状态 |
|---|---|---|---|---|
| - | - | - | （暂无） | - |

---

## 变更记录

| 日期 | 内容 | 操作人 |
|---|---|---|
| 2026-06-24 | 初版进度跟踪文档创建 | - |
| 2026-06-24 | 从 `feature/lowcode-phase-1-entity-metadata` 派生分支 `feature/lowcode-phase-2-release-codegen`，完成 M1 全部 18 项；`mvn compile` 通过 | - |
| 2026-06-24 | 完成 M2 后端 M2-01~06：Menu 实体 + Repository + MenuService + MenuController；MountPhase 接入 `cloneDraftToRelease`；SnapshotPhase 增加 `menu` ReleaseItem；`mvn compile` 通过 | - |
| 2026-06-24 | M2 后端冒烟测试 `back/scripts/m2-smoke.http` 全量通过（6 章 14 请求）：草稿 CRUD / reorder / 五阶段发布 / 35 ReleaseItems 快照 / active 菜单读取 / 草稿改动隔离 / 回滚校验全部 OK | - |
| 2026-06-24 | 修复冒烟暴露的两个问题：① `ReleaseService` / `MenuService` 业务校验改用 `IllegalArgument/IllegalStateException`，真实错误信息可透传不再被 GlobalExceptionHandler 兜底成"操作失败"；② 一次性脚本 `back/scripts/migrate-utf8mb4.sql`（存储过程游标逐表 ALTER）把 lc_* 系列表升 utf8mb4，emoji + 中文写入读出正常 | - |
